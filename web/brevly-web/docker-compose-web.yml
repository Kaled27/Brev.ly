services:
  app:
    container_name: brevly-web
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_URL: http://localhost:3333/
    ports:
      - "5173:80"
    restart: always
